version: '3.8'

services:
  absensi-app:
    build: .
    container_name: absensi-app
    ports:
      - "3000:3000"
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
